# AutoReconnectMin（v0.8.0-min）测试流程

> 目的：验证「谁断线谁重连，重连完毕后**全队回退到最近检查点**」这一 Min 版核心痛点是否达成：
> **一个人掉线 → 全队返回大厅 → 加载同一份干净对局 → 从最近节点重打**，且不再出现 StateDivergence（状态分歧）。

---

## 0. 机制简述（测试前必读）

- **触发者**：只有**主机**会在检测到客机掉线时触发回退。钩子在 `RunLobby.OnDisconnectedFromClientAsHost`。
- **触发延迟**：Steam 传输层超时约 **14 秒**后主机才会收到掉线事件。**掉线后请至少等 15 秒再看效果**，不要提前判定失败。
- **触发条件**（全部满足才回退）：
  1. 设置里「自动重连(Min)」总开关 = 开，且 `checkpointRollbackEnabled` = 开；
  2. 本机是**主机**且在对局中（`NetService.Type == Host`）；
  3. 当前已有可回退检查点（`CheckpointStore.Latest != null`，即至少经历过一次节点存档）；
  4. 掉线原因 **不是** `RunInProgress`（那是主机主动拒绝“运行中重连”，并非真实掉线，跳过以免死循环）。
- **回退动作**：主机 `ReturnToMainMenuAfterRun` → 全员退回主菜单 → 主机以检查点重新托管（`StartSteamHost`）→ 推送原生多人读档界面（`InitializeAsHost(checkpoint)`）。
- **关键含义**：
  - **全员（含主机、含未掉线的其他客机）都会被拽回主菜单并加载同一份检查点**——这是设计使然，不是 bug。
  - 检查点是**上一次存档边界**（进节点/战斗结算后），所以回退会丢失「该存档点之后的进度」，回到干净节点重打。
  - **掉线玩家需重新加入主机的新托管会话**（见场景步骤）。
- **不覆盖的场景（已知限制）**：
  - **主机自己掉线**：没有主机来触发回退，本 Min 版不处理（全员会话结束，需重新开黑）。
  - 回退后其他未掉线客机是否**自动**重进新大厅，需实测确认（见场景 A 验证点 4）。

---

## 1. 前置准备

- 至少 **2 人**（1 主机 + 1 客机），建议 **3 人**更贴近真实；各自独立 Steam 账号。
- 所有参与机都装上 **`AutoReconnectMin` v0.8.0-min**（见第 2 节部署校验）。
- **默认版 `AutoReconnect` 槽位**：Min 测试期间可空着，也可另行装 master 版——两者 mod id 不同（默认 `AutoReconnect` / Min `AutoReconnectMin`），**可共存、互不覆盖**。
- 关闭其他可能干扰联机的 mod（尤其是别的重连/托管类 mod）。
- 三机时间、Steam 在线状态正常，能互相发好友邀请。

---

## 2. 部署校验（开打前，每台机都做）

1. 游戏目录确认存在：
   `…\Slay the Spire 2\mods\AutoReconnectMin\AutoReconnectMin.dll`
   `…\Slay the Spire 2\mods\AutoReconnectMin\AutoReconnectMin.json`
2. 启动游戏 → 设置 → **Mods**，应能看到 **「自动重连(Min)」** 条目（与默认版「自动重连」并列）。
3. 确认开关：**总开关 = 开**；展开后 `checkpointRollbackEnabled`（检查点回退）也 = 开。
4. 确认日志文件已生成：`%APPDATA%\SlayTheSpire2\mods\AutoReconnectMin\logs\autoreconnectmin.log`
   打开后应能看到启动日志含 `AutoReconnect v0.8.0-min (Checkpoint Rollback)`。

> 任一机不满足则先修部署，不要带病测试。

---

## 3. 冒烟测试：检查点捕获是否生效

**目标**：确认 `CheckpointStore` 确实在节点边界捕获了检查点，否则掉线时回退会因“无检查点”被跳过。

步骤（主机视角）：
1. 主机开一局多人，客机加入。
2. 走过 **至少 2 个节点**（例如：进战打一场 → 结算 → 走到下一个节点）。
3. 打开 `autoreconnectmin.log`，搜索 `[Checkpoint] 已捕获检查点`。

**通过标准**：日志中出现 `已捕获检查点（共 N 个，玩家数 M）` 且 `N ≥ 2`、`M` = 实际玩家数。
**失败**：没有任何捕获日志 → 捕获钩子未生效，先排查 `CheckpointCapturePatch` / 部署。

> 小技巧：每个节点切换、每场战斗结算都应产生一次捕获，所以打几步后 `N` 应明显 > 1。

---

## 4. 核心场景测试

### 场景 A：地图/节点浏览时客机掉线（最典型痛点）

1. 主机 + 客机甲 + 客机乙 在**地图/节点选择界面**（不在战斗中），已走过 ≥2 节点。
2. **客机甲**强制掉线：直接 Steam 退出 / 任务管理器结束游戏 / 拔网线（任选一种“硬掉线”最贴近真实）。
   - ⚠️ 不要用游戏内「离开对局」正常退出（那是 RunInProgress 主动拒绝，会被 Min 版跳过，不算真实掉线）。
3. **主机等 ≥15 秒**，观察主机屏幕与 `autoreconnectmin.log`：
   - 期望日志：`[Checkpoint] 主机检测到玩家 <甲ID> 掉线（<reason>），触发全队回退到最近检查点。`
   - 期望日志：`[Checkpoint] 开始全队回退到最近检查点…` → `已推送检查点读档界面…`
   - 期望画面：主机与仍在线的客机乙**都被送回主菜单**，并弹出/进入「从检查点读档」的多人读档界面，显示的是**最近节点**状态。
4. **全员重连检查点**：
   - 主机在新托管会话里向甲、乙重新发 Steam 邀请（或游戏内重邀）。
   - 甲、乙加入后，三人应处于**同一份检查点**（最近干净节点），从那里继续。
5. 验证无分歧：继续推进一段（再走 1~2 节点 / 打一场），**不出现**「Abandoning run and returning to main menu」或状态分歧弹窗。

**通过标准**：甲掉线后，主机+乙无分歧地全员回到最近节点并重打；甲重连后三人状态一致、可继续。
**重点验证点 4**：乙（未掉线客机）是**自动**重进新大厅，还是需要手动重连？记录实测结果——这决定后续是否要补「未掉线客机自动重连」逻辑。

---

### 场景 B：战斗进行中客机掉线

1. 主机 + 客机甲 进入一场**战斗**（combat 进行中）。
2. **客机甲**硬掉线（同场景 A 方式）。
3. 主机等 ≥15 秒，观察：
   - 期望：回退触发，**全员回到“这场战斗开始前的节点”**（因为检查点是节点边界，不含进行中战斗），该战斗需要**重打**。
   - 期望日志同场景 A。
4. 甲重连后，三人从战斗前节点进入并**重打这场战斗**。
5. 验证：重打过程无分歧、可正常结算。

**通过标准**：战斗内掉线 = 全队回退到战斗前节点并重打，而非“把战斗现场硬塞进重连”（这正是 Min 版规避 StateDivergence 的设计目标）。
**注意**：这是预期行为，不是数据丢失——只是该战斗进度回滚。

---

### 场景 C：开关关闭验证（回归默认行为）

1. 主机把 `checkpointRollbackEnabled` 关掉，重开一局。
2. 客机硬掉线。
3. 主机等 ≥15 秒：
   - 期望日志：`[Checkpoint] 忽略…不触发回退`（或根本无触发日志）。
   - 期望：主机**不再**把全队拽回主菜单；保留原有单人重连/托管逻辑。

**通过标准**：关闭开关后，Min 版回退行为彻底不触发，且不影响其他功能。

---

### 场景 D：主机自己掉线（预期失败，记录现象）

1. **主机**硬掉线，客机甲在线。
2. 观察：预期 **不会有任何回退**（没有主机来触发 `OnDisconnectedFromClientAsHost`）。
3. 记录客机甲看到的现象（通常是会话结束/被送回主菜单/提示主机离开）。

**通过标准（记录项）**：确认 Min 版对“主机掉线”无能为力——这是已知限制，便于后续决定是否要补主机迁移方案。

---

## 5. 日志验证要点速查

| 想确认的事 | 搜日志关键字 |
|---|---|
| 检查点被捕获 | `已捕获检查点（共` |
| 主机检测到掉线并触发回退 | `主机检测到玩家` + `触发全队回退` |
| 回退开始/完成 | `开始全队回退` / `已推送检查点读档界面` |
| 被 RunInProgress 跳过 | `忽略` + `RunInProgress` |
| 无检查点被跳过 | `尚无可回退检查点` |
| 回退异常 | `回退执行异常` / `回退触发异常` |
| 非主机/重入守卫 | `非主机上下文，跳过` / `已有回退在进行` |

日志路径：`%APPDATA%\SlayTheSpire2\mods\AutoReconnectMin\logs\autoreconnectmin.log`（每台机各看各的）。

---

## 6. 通过标准汇总

- ✅ 冒烟：检查点捕获日志 `N ≥ 2`。
- ✅ 场景 A：客机掉线 → 主机+其他客机无分歧地全员回退到最近节点；掉线者重连后三人状态一致。
- ✅ 场景 B：战斗内掉线 → 回退到战斗前节点并重打，无 StateDivergence。
- ✅ 场景 C：关开关后回退不触发，其余功能正常。
- ✅ 场景 D：主机掉线现象已记录（已知不处理）。
- ❌ 任一场景出现「Abandoning run / 状态分歧弹窗」= Min 版未达成目标，需回看日志定位。

---

## 7. 回滚/降级预案

- 若测试中出现严重卡死（如全员卡在主菜单无法重连）：
  1. 全员彻底退出游戏；
  2. 主机在设置里把 `checkpointRollbackEnabled` 关掉，或临时移出 `mods/AutoReconnectMin/`；
  3. 重新开黑，恢复正常默认联机。
- 默认版 `AutoReconnect`（master 分支）不受影响，可随时切回。

---

## 8. 实测后请回填

- 场景 A 验证点 4 结论（乙是否自动重连）：__________
- 场景 B 回退落点是否符合预期：__________
- 掉线原因 `reason` 实际取值（日志里）：__________
- 是否出现任何分歧/异常：__________
- 其他观察：__________
